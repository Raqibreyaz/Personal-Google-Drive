TODO:
  - upgrading/downgrading plan
  - return free plan subscription from /get-subscription when no subscription happened yet
  - import from google drive
  - directory sharing functionality with the help of path property
  - Bulk Download
  - Download as zip for both files and directories
  - sort by name,date,size etc.
  - filter

LATER:
  - check for atomic Ops requirement wherever possible
  - show subscribed plan as marked
  - make the subscription creation and user updation atomic
  - ErrorCode fixing in Error Responses
  - Move/copy files/directories from one directory to other
  - fix downloading and filenaming problem
  - allow upload when consumed space comes under free plan but wait... data can be lost on cron job? no, we are not deleting user's files yet
  - put user's files to glacier storage for low cost where needed
  - handle case where user has paused subscription from too long

DONE:
  - Bulk Delete
  - s3 file upload from frontend
  - get file functionality using s3
  - cloudfront cache invalidation on object deletion
  - intelligent tiering
  - payment_gateway_integration:
      - show 3 plans: basic,standard and pro
      - every plan will have 2 versions: monthly and yearly
      - add plan in razorpay dashboard accordingly
  - increasing user's maxStorageBytes on subscription
  - double click to view directory or file
  - single click to select and ctrl + single-click to select multiple
  - use OAuth2.0 for gmail nodemailer
  - stop user from subscribing an already subscribed plan
  - should i store razorpay events in DB?yes
  - handle all necessary subscription events
  - add daily cron job to handle expired graced subscriptions
  - dont allow uploads on states awaiting_activation,past_due,in_grace,paused
  - for cancelled state, dont allow uploading after grace period over
  - grace expiry can be of pending subscription's grace expiry or cancelled subscription's grace expiry, so handle both? yes, also mark in_grace as cancelled then
  - i dont think grace period of cancel should block file upload as user will have subscription available till it's renewal time? yes, fixed
  - limit user action on basis of subscription states, done by handling in_grace,cancelled etc.
  - paused:
      - block uploads
      - allow downloads but till a particular time
      - block all access after that particular time ends

  - activated:
      - update subscription's status, start and end time in db
      - update user's maxStorageBytes
  - cancelled:
      - downgraded the user to free plan after the grace period expiry
  - past_due & in_grace:
      - alert the user and start retry/grace logic
